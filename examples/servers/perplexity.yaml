# Perplexity MCP Server
# Provides AI-powered search and research tools
#
# Required secrets in .env:
#   EMCP_PERPLEXITY_SECRET=pplx-xxxxx
#
# 1. Add the service block below to docker-compose.yaml under 'services:'
# 2. Save the JSON config to configs/perplexity.json
# 3. Run: docker compose up -d perplexity-mcp
# 4. Register: docker exec emcp-server /mcpjungle register -c /configs/perplexity.json

# --- docker-compose service ---
# perplexity-mcp:
#   image: mcp/perplexity-ask:latest
#   container_name: perplexity-mcp
#   command: ["node", "dist/index.js"]
#   stdin_open: true
#   tty: true
#   environment:
#     - PERPLEXITY_API_KEY=${EMCP_PERPLEXITY_SECRET}
#   networks:
#     - emcp-network
#   restart: unless-stopped

# --- configs/perplexity.json ---
# {
#     "name": "perplexity",
#     "transport": "stdio",
#     "description": "Perplexity AI search and research",
#     "command": "docker",
#     "args": ["exec", "-i", "perplexity-mcp", "node", "dist/index.js"]
# }
