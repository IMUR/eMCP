# Svelte MCP Server
# Provides Svelte framework tools and component assistance
# No API keys required
#
# 1. Add the service block below to docker-compose.yaml under 'services:'
# 2. Save the JSON config to configs/svelte.json
# 3. Run: docker compose up -d svelte-mcp
# 4. Register: docker exec emcp-server /mcpjungle register -c /configs/svelte.json

# --- docker-compose service ---
# svelte-mcp:
#   image: oven/bun:1
#   container_name: svelte-mcp
#   command: ["bunx", "@sveltejs/mcp"]
#   stdin_open: true
#   tty: true
#   networks:
#     - emcp-network
#   restart: unless-stopped

# --- configs/svelte.json ---
# {
#     "name": "svelte",
#     "transport": "stdio",
#     "description": "Svelte framework tools",
#     "command": "docker",
#     "args": ["exec", "-i", "svelte-mcp", "bunx", "@sveltejs/mcp"]
# }
