# ElevenLabs MCP Server
# Provides text-to-speech and voice tools
#
# Required secrets in .env:
#   ELEVENLABS_API_KEY=your_elevenlabs_key
#
# 1. Add the service block below to docker-compose.yaml under 'services:'
# 2. Save the JSON config to configs/elevenlabs.json
# 3. Run: docker compose up -d elevenlabs-mcp
# 4. Register: docker exec emcp-server /mcpjungle register -c /configs/elevenlabs.json

# --- docker-compose service ---
# elevenlabs-mcp:
#   image: mcp/elevenlabs:latest
#   container_name: elevenlabs-mcp
#   command: ["elevenlabs-mcp"]
#   stdin_open: true
#   tty: true
#   environment:
#     - ELEVENLABS_API_KEY=${ELEVENLABS_API_KEY}
#   networks:
#     - emcp-network
#   restart: unless-stopped

# --- configs/elevenlabs.json ---
# {
#     "name": "elevenlabs",
#     "transport": "stdio",
#     "description": "ElevenLabs text-to-speech and voice tools",
#     "command": "docker",
#     "args": ["exec", "-i", "elevenlabs-mcp", "elevenlabs-mcp"]
# }
