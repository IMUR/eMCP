# GitHub MCP Server
# Provides GitHub API tools (repos, issues, PRs, code search, etc.)
#
# Required secrets in .env:
#   EMCP_GITHUB_SECRET=github_pat_xxxxx
#
# 1. Add the service block below to docker-compose.yaml under 'services:'
# 2. Save the JSON config to configs/github.json
# 3. Run: docker compose up -d github-mcp
# 4. Register: docker exec emcp-server /mcpjungle register -c /configs/github.json

# --- docker-compose service ---
# github-mcp:
#   image: ghcr.io/github/github-mcp-server:latest
#   container_name: github-mcp
#   command: ["stdio"]
#   stdin_open: true
#   tty: true
#   environment:
#     - GITHUB_PERSONAL_ACCESS_TOKEN=${EMCP_GITHUB_SECRET}
#   restart: always
#   networks:
#     - emcp-network

# --- configs/github.json ---
# {
#     "name": "github",
#     "transport": "stdio",
#     "description": "GitHub integration",
#     "command": "docker",
#     "args": ["exec", "-i", "github-mcp", "/server/github-mcp-server", "stdio"]
# }
