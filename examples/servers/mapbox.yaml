# Mapbox MCP Server
# Provides mapping, geocoding, and directions tools
#
# Required secrets in .env:
#   MAPBOX_PUBLIC_API_KEY=pk.xxxxx
#
# 1. Add the service block below to docker-compose.yaml under 'services:'
# 2. Save the JSON config to configs/mapbox.json
# 3. Run: docker compose up -d mapbox-mcp
# 4. Register: docker exec emcp-server /mcpjungle register -c /configs/mapbox.json

# --- docker-compose service ---
# mapbox-mcp:
#   image: mcp/mapbox:latest
#   container_name: mapbox-mcp
#   command: ["node", "dist/esm/index.js"]
#   stdin_open: true
#   tty: true
#   environment:
#     - MAPBOX_ACCESS_TOKEN=${MAPBOX_PUBLIC_API_KEY}
#   networks:
#     - emcp-network
#   restart: unless-stopped

# --- configs/mapbox.json ---
# {
#     "name": "mapbox",
#     "transport": "stdio",
#     "description": "Mapbox mapping and geocoding tools",
#     "command": "docker",
#     "args": ["exec", "-i", "mapbox-mcp", "node", "dist/esm/index.js"]
# }
